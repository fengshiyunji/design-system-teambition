import { Meta, Preview } from '@storybook/addon-docs/blocks';
import { Checkbox } from '../../components';
import '../../styles/stories.scss';

<Meta title="components/Checkbox" component={Checkbox} />

# Checkbox

Checkbox component.

## Basic Checkbox

<Preview>
    <Checkbox defaultChecked={false} onChange={(e)=> { console.log(`checked = ${e.target.checked}`) } }>
        Checkbox1
    </Checkbox>
    <Checkbox defaultChecked onChange={(e)=> { console.log(`checked = ${e.target.checked}`) } }>
        Checkbox2
    </Checkbox>
</Preview>

## Disabled Checkbox

<Preview>
    <Checkbox defaultChecked={false} disabled > Disabled Checkbox1 </Checkbox>
    <Checkbox defaultChecked disabled > Disabled Checkbox2 </Checkbox>
</Preview>